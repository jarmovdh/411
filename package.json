{
  "scripts": {
    "start": "cd 411-backend && npm start",
    "heroku-postbuild": "cd 411-backend && npm install --legacy-peer-deps && npm run build && medusa migrations run"
  },
  "devDependencies": {
    "@svgr/webpack": "^8.1.0"
  },
  "dependencies": {
    "@medusajs/cache-redis": "^1.9.2",
    "@medusajs/event-bus-redis": "^1.8.14"
  }
}